import * as ReactSimpleMaps from '@gameworkers/react-simple-maps';
import * as Redux from 'redux';
import * as NextReduxWrapper from 'next-redux-wrapper';
import * as ReduxTooltip from 'redux-tooltip';
import * as ReactRedux from 'react-redux';

Object.assign(window, {
  ReactSimpleMaps,
  Redux,
  NextReduxWrapper,
  ReduxTooltip,
  ReactRedux
});
